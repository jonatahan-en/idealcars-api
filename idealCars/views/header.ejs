<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= __('appName') %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <meta name="description" content="<%= __('appDescription') %>">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
</head>
<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top shadow-sm">
    <div class="container-fluid">
        <a class="navbar-brand fw-bold" href="/"> 
            <img src="/logo/Logo_IdealCars.png" alt="<%= __('appName') %> Logo" height="30"> 
        </a>
        
        <div class="d-flex order-lg-last">
            <button id="theme-toggle" class="btn btn-outline-secondary me-2" type="button">
                <i id="theme-icon" class="bi bi-moon-fill"></i>
            </button>
            
            <div class="dropdown me-2">
                <button class="btn btn-outline-secondary dropdown-toggle d-flex align-items-center" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <img src="/flags/<%= currentLocale === 'en' ? 'en' : currentLocale === 'es' ? 'es' : 'prt' %>.png" 
                         alt="<%= currentLocale %>" width="20" height="15" class="me-1">
                    <span class="d-none d-sm-inline">
                        <%= currentLocale === 'en' ? 'English' : currentLocale === 'es' ? 'Español' : 'Português' %>
                    </span>
                </button>
                <ul class="dropdown-menu dropdown-menu-end">
                    <% ['en', 'es', 'pt'].forEach(lang => { %>
                        <% if (currentLocale !== lang) { %>
                            <li><a class="dropdown-item d-flex align-items-center" href="?lang=<%= lang %>">
                                <img src="/flags/<%= lang === 'pt' ? 'prt' : lang %>.png" alt="<%= lang %>" width="20" height="15" class="me-2">
                                <%= lang === 'en' ? 'English' : lang === 'es' ? 'Español' : 'Português' %>
                            </a></li>
                        <% } %>
                    <% }); %>
                </ul>
            </div>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent" aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
        </div>
        <div class="collapse navbar-collapse" id="navbarContent">
            <div class="d-flex flex-column flex-lg-row w-100">
                <div class="d-flex flex-column flex-lg-row me-lg-auto">
                    <a class="btn btn-primary my-1 mx-lg-1" href="/">
                        <i class="bi bi-house-door"></i> 
                        <span class="ms-1"><%= __('navigation.home') %></span>
                    </a>
                    <a class="btn btn-primary my-1 mx-lg-1" href="/products/new">
                        <i class="bi bi-plus-circle"></i> 
                        <span class="ms-1"><%= __('navigation.newAd') %></span>
                    </a>
                </div>
                <div class="d-flex flex-column flex-lg-row">
                    <% if(session.userId) { %>
                        <a href="/myproducts" class="btn btn-outline-info my-1 mx-lg-1">
                            <i class="bi bi-box-seam"></i> 
                            <span class="ms-1"><%= __('navigation.myProducts') %></span>
                        </a>
                        <a href="/logout" class="btn btn-outline-success my-1 mx-lg-1">
                            <i class="bi bi-box-arrow-right"></i> 
                            <span class="ms-1"><%= __('navigation.logout') %></span>
                        </a>
                        <a href="/signout" class="btn btn-outline-danger my-1 mx-lg-1">
                            <i class="bi bi-person-x"></i> 
                            <span class="ms-1"><%= __('navigation.signout') %></span>
                        </a>
                    <% } else { %>
                        <a href="/login" class="btn btn-outline-success my-1 mx-lg-1">
                            <i class="bi bi-box-arrow-in-right"></i> 
                            <span class="ms-1"><%= __('navigation.login') %></span>
                        </a>
                        <a href="/signup" class="btn btn-outline-primary my-1 mx-lg-1">
                            <i class="bi bi-person-plus"></i> 
                            <span class="ms-1"><%= __('navigation.signup') %></span>
                        </a>
                    <% } %>
                </div>
            </div>
        </div>
    </div>
</nav>