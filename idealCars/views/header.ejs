<!DOCTYPE html>
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= __('appName') %></title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.5/dist/css/bootstrap.min.css" rel="stylesheet" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/css/styles.css">
    <meta name="description" content="<%= __('appDescription') %>">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
</head>
<body>
<nav class="navbar navbar-expand-lg bg-body-tertiary sticky-top shadow-sm">
    <div class="container">
        <a class="navbar-brand fw-bold" href="/"> 
            <img src="/logo/Logo_IdealCars.png" alt="<%= __('appName') %> Logo" height="30"> 
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0 gap-1">
                <li class="nav-item">
                    <a class="btn btn-primary btn-sm" href="/"> <i class="bi bi-house-door"></i> <%= __('navigation.home') %></a>
                </li>
                <% if(session.userId) { %>
                <li class="nav-item">
                    <a class="btn btn-primary btn-sm" href="/products/new"> <i class="bi bi-plus-circle"></i> <%= __('navigation.newAd') %></a>
                </li>
                <% } %>
            </ul>
            <div class="d-flex align-items-center gap-2 flex-wrap">
                <!-- Botón de cambio de tema (claro/oscuro) -->
                <button id="theme-toggle" class="btn btn-outline-secondary btn-sm" type="button">
                    <i id="theme-icon" class="bi bi-moon-fill"></i>
                </button>
                
                <% if(session.userId) {%>
                    <div class="dropdown d-lg-none mb-2">
                        <button class="btn btn-sm btn-outline-primary dropdown-toggle" type="button" id="userMenuMobile" data-bs-toggle="dropdown" aria-expanded="false">
                            <i class="bi bi-person"></i> Mi cuenta
                        </button>
                        <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="userMenuMobile">
                            <li><a href="/myproducts" class="dropdown-item"><i class="bi bi-box-seam"></i> <%= __('navigation.myProducts') %></a></li>
                            <li><a href="/logout" class="dropdown-item"><i class="bi bi-box-arrow-right"></i> <%= __('navigation.logout') %></a></li>
                            <li><a href="/signout" class="dropdown-item"><i class="bi bi-person-x"></i> <%= __('navigation.signout') %></a></li>
                            <li><a href="/profile" class="dropdown-item"><i class="bi bi-person"></i> Perfil</a></li>
                        </ul>
                    </div>
                    
                    <div class="d-none d-lg-flex gap-1">
                        <a href="/myproducts" class="btn btn-outline-info btn-sm" type="button"> <i class="bi bi-box-seam"></i> <%= __('navigation.myProducts') %></a>
                        <a href="/logout" class="btn btn-outline-success btn-sm" type="button"> <i class="bi bi-box-arrow-right"></i> <%= __('navigation.logout') %></a>
                        <a href="/signout" class="btn btn-outline-danger btn-sm" type="button"> <i class="bi bi-person-x"></i> <%= __('navigation.signout') %></a>
                    </div>
                <% } else { %>
                    <div class="d-flex gap-1">
                        <a href="/login" class="btn btn-outline-success btn-sm" type="button"> <i class="bi bi-box-arrow-in-right"></i> <%= __('navigation.login') %></a>
                        <a href="/signup" class="btn btn-outline-primary btn-sm" type="button"> <i class="bi bi-person-plus"></i> <%= __('navigation.signup') %></a>
                    </div>
                <% } %>
                
                <div class="dropdown ms-1">
                    <button class="btn btn-outline-secondary btn-sm dropdown-toggle d-flex align-items-center" type="button" id="languageDropdown" data-bs-toggle="dropdown" aria-expanded="false">
                        <img src="/flags/<%= currentLocale === 'en' ? 'en' : currentLocale === 'es' ? 'es' : currentLocale === 'pt' ? 'prt' : 'en' %>.png" alt="<%= currentLocale %>" width="16" height="12" class="me-1">
                        <span class="d-none d-md-inline fw-bold text-capitalize">
                            <%= currentLocale === 'en' ? 'English' : currentLocale === 'es' ? 'Español' : currentLocale === 'pt' ? 'Português' : currentLocale %>
                        </span>
                    </button>
                    <ul class="dropdown-menu dropdown-menu-end" aria-labelledby="languageDropdown">
                        <% if (currentLocale !== 'en') { %>
                            <li><a class="dropdown-item d-flex align-items-center" href="?lang=en">
                                <img src="/flags/en.png" alt="English" width="16" height="12" class="me-2"> English
                            </a></li>
                        <% } %>
                        <% if (currentLocale !== 'es') { %>
                            <li><a class="dropdown-item d-flex align-items-center" href="?lang=es">
                                <img src="/flags/es.png" alt="Español" width="16" height="12" class="me-2"> Español
                            </a></li>
                        <% } %>
                        <% if (currentLocale !== 'pt') { %>
                            <li><a class="dropdown-item d-flex align-items-center" href="?lang=pt">
                                <img src="/flags/prt.png" alt="Português" width="16" height="12" class="me-2"> Português
                            </a></li>
                        <% } %>
                    </ul>
                </div>
            </div>
        </div>
    </div>
</nav>